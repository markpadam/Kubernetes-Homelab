apiVersion: apps/v1
kind: Deployment
metadata:
  name: cosmosdb
  namespace: cosmos-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cosmosdb
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cosmosdb
    spec:
      containers:
        - name: cosmosdb
          image: mcr.microsoft.com/cosmosdb/linux/azure-cosmos-emulator:vnext-preview
          ports:
            - containerPort: 8081
              name: nosql
            - containerPort: 8080
              name: health
            - containerPort: 1234
              name: explorer
          env:
            - name: PROTOCOL
              value: "http"
            - name: GATEWAY_PUBLIC_ENDPOINT
              value: "cosmosdb.cosmos-db.svc.cluster.local"
            - name: DATA_PATH
              value: "/usr/cosmos/data"
            - name: ENABLE_TELEMETRY
              value: "false"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /alive
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 20
          volumeMounts:
            - name: cosmos-data
              mountPath: /usr/cosmos/data
      volumes:
        - name: cosmos-data
          persistentVolumeClaim:
            claimName: cosmosdb-data
