apiVersion: v1
kind: Service
metadata:
  name: incidenthub-web
  namespace: {{ .Values.namespace }}
spec:
  selector: { app.kubernetes.io/name: incidenthub, component: web }
  ports:
    - port: 80
      targetPort: {{ .Values.web.port }}
      protocol: TCP
