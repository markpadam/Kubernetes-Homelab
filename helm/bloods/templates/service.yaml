apiVersion: v1
kind: Service
metadata:
  name: bloods-web
  namespace: {{ .Values.namespace }}
spec:
  selector: { app.kubernetes.io/name: bloods, component: web }
  ports:
    - port: 80
      targetPort: {{ .Values.web.port }}
      protocol: TCP
