apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Prd environment overlay. Starts empty (apps are managed by lab-feature.sh).
# When prd diverges from dev, list app overlays explicitly here instead.
resources: []
