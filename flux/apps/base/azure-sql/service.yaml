apiVersion: v1
kind: Service
metadata:
  name: mssql
  namespace: azure-sql
spec:
  type: LoadBalancer
  loadBalancerIP: 172.16.3.3
  selector:
    app: mssql
  ports:
    - name: mssql
      port: 1433
      targetPort: 1433
