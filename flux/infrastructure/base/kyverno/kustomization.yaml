apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  # sample-policies.yaml is applied separately by lab-feature.sh once Kyverno
  # admission controller is up — applying it via kustomize would race the CRDs.
