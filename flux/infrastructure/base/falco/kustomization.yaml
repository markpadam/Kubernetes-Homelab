apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  # ingress.yaml is applied separately by lab-feature.sh once the falcosidekick
  # UI service exists.
